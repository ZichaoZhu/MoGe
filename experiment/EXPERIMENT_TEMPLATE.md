# 阶段内运行：实验简称

> 新运行保存在对应阶段的 `runs/` 下。只有形成新的独立研究问题时才新增阶段。

## 实验目标

说明本次实验的假设，以及为什么需要进行该实验。

## 相较阶段基线的变更

列出本次实验的自变量，不要混入无关改动。

## 实验配置

- 实现代码提交：
- 数据：
- 模型：
- 可训练模块：
- 分辨率与批量大小：
- 优化器：
- 损失：
- 硬件：

## 实验结果

记录基线与最终指标、运行时间、峰值显存，以及实验是否在没有数值错误的情况下完成。

## 实验结论

明确本次结果能够证明什么，以及尚不能证明什么。

## 复现方法

将完整运行命令保存在 `run.sh`，结构化参数保存在 `config.json`，原始指标保存在
`metrics/report.json`。

## 实验产物

体积较小的实验产物应提交到 Git。大型检查点和数据集样本保存在服务器上的对应阶段
目录中，通过 `.gitignore` 排除，并记录其 SHA-256 校验和及服务器绝对路径。运行
完成后更新阶段 `README.md`；同一研究问题下的短诊断不再创建新的顶层实验编号。
